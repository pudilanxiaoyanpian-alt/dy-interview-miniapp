<view class="page">
  <view class="nav">
    <view class="brand">
      <image class="logo" src="/images/logo.png" mode="aspectFill" />
      <text class="brand-name">{{brand}}</text>
    </view>
    <button class="nav-action" bindtap="onPrimaryTap">免费测评</button>
  </view>

  <view class="hero-section">
    <view class="badge">
      <view class="badge-dot"></view>
      <text>{{badgeMain}}</text>
      <view class="badge-separator"></view>
      <text class="badge-highlight">{{badgeSub}}</text>
    </view>

    <view class="hero">
      <text class="title-line">{{titlePrefix}}</text>
      <text class="title-line"><text class="title-highlight">{{titleHighlight}}</text> {{titleSuffix}}</text>
      <text class="title-line">{{titleSecondLine}}</text>
    </view>

    <text class="description">{{description}}</text>

    <view class="actions">
      <button class="primary-button" bindtap="onPrimaryTap">
        <text>开始免费测评</text>
        <text class="arrow">→</text>
      </button>
      <button class="secondary-button" bindtap="onFeatureTap">了解功能</button>
    </view>
  </view>

  <view id="features" class="features-section">
    <view class="section-head">
      <text class="section-title">覆盖求职准备关键环节</text>
      <text class="section-subtitle">先测问题，再补短板，把简历和面试准备落到具体动作上</text>
    </view>

    <view class="feature-list">
      <view tt:for="{{features}}" tt:key="title" class="feature-card">
        <view class="feature-icon {{item.tone}}">
          <text>{{item.code}}</text>
        </view>
        <text class="feature-title">{{item.title}}</text>
        <text class="feature-description">{{item.description}}</text>
      </view>
    </view>
  </view>

  <view class="footer">
    <view class="footer-brand">
      <image class="footer-logo" src="/images/logo.png" mode="aspectFill" />
      <text>{{brand}}</text>
    </view>
    <view class="footer-links">
      <button
        tt:for="{{footerLinks}}"
        tt:key="name"
        class="footer-link"
        data-path="{{item.path}}"
        bindtap="onFooterLinkTap"
      >
        {{item.name}}
      </button>
    </view>
    <text class="copyright">© 2026 求职有谱儿 · All rights reserved.</text>
  </view>
</view>
